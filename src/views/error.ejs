<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>HireSense Backend API</title>
  <style>
    body { font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif; margin: 0; padding: 48px; color: #1f2933; background: #f5f7fa; }
    .card { max-width: 560px; margin: 0 auto; background: #fff; border-radius: 12px; padding: 32px; box-shadow: 0 1px 3px rgba(0,0,0,.08); }
    h1 { margin: 0 0 8px; font-size: 20px; }
    p { margin: 6px 0; color: #52606d; }
    code { background: #eef2f7; padding: 2px 6px; border-radius: 4px; }
  </style>
</head>
<body>
  <div class="card">
    <h1>HireSense Backend API</h1>
    <p>The service is running.</p>
    <p>All endpoints are <code>POST</code> under <code>/v1</code>. Auth routes live at <code>/v1/user/*</code>.</p>
  </div>
</body>
</html>
